name: e2e-autoscale-test

services:
  cpu-worker:
    image: alpine:latest
    command: ["sleep", "3600"]
    deploy:
      replicas: 1
      autoscale:
        min: 1
        max: 3
        target_cpu: 50
        cooldown: 15s
